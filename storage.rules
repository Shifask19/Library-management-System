
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // By default, deny all reads and writes
    // This is a secure starting point. Add specific rules below.

    // Example: Allow authenticated users to read/write their own files in a specific folder
    // match /userFiles/{userId}/{allPaths=**} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }

    // Example: Allow public read access to book cover images
    // match /bookCovers/{allPaths=**} {
    //  allow read: if true;
    //  allow write: if request.auth != null; // Or only admins: if get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    // }

    // Default: Deny all access to any other path
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

    